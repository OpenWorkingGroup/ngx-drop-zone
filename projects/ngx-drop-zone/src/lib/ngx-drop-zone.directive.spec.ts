import { NgxDropZoneDirective } from './ngx-drop-zone.directive';

describe('NgxDropZoneDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxDropZoneDirective();
    expect(directive).toBeTruthy();
  });
});
